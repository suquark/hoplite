syntax = "proto3";

package objectstore;

message PullRequest {
  bytes object_id = 1;
  bytes puller_ip = 2;
}

message PullReply {
}

service ObjectStore {
  rpc Pull(PullRequest) returns (PullReply);
}
